apply plugin: "war"

bootWar {
    mainClassName = "xyz.doubleedgetech.MhipsteropenidApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
